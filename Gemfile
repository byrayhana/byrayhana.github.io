source "https://rubygems.org"
# GitHub Pages compatible build (matches the versions GitHub Pages runs server-side)
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"   # needed for local `jekyll serve` on Ruby 3+
